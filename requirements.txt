# ============================================================
# requirements.txt — pinned to tested versions.
# Lower bound = minimum required API; upper bound = next major
# (prevents silent breaking changes on fresh installs).
# Run `pip install -r requirements.txt` to reproduce exactly.
# ============================================================

# PDF extraction
PyMuPDF>=1.24.0,<2.0

# DOCX extraction
python-docx>=1.1.0,<2.0

# RTF extraction
striprtf>=0.0.26,<1.0

# OCR for scanned documents
pytesseract>=0.3.10,<1.0
Pillow>=10.0.0,<13.0

# Data validation
pydantic>=2.0.0,<3.0

# Settings / .env loading
pydantic-settings>=2.0.0,<3.0

# Embedding model
sentence-transformers>=2.2.0,<6.0

# Vector store
chromadb>=0.4.0,<1.0

# API server
fastapi>=0.100.0,<1.0
uvicorn[standard]>=0.20.0,<1.0
httpx>=0.24.0,<1.0
python-multipart>=0.0.6,<1.0
